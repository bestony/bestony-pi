// Keep the tests discoverable by the default `node --test` command. The
// implementation remains beside the workflow so the workflow and its tests
// stay easy to find together.
import "../.github/scripts/generate-release-notes.test.mjs";
